const editButton = document.querySelector('.profile__edit-button');
const popup = document.querySelector(selectors: '.popup');
editButton.addEventListener('click', listner: function()) {
    popup.classList.add('popup__is-open');
}